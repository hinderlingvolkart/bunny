(function () {
  if (Node.prototype.firstElementChild === undefined) {
    Object.defineProperty(Node.prototype, 'firstElementChild', {
      get: function get() {
        var childNodes = this.childNodes;
        var length = childNodes.length;
        for (var k = 0; k < length; k++) {
          var node = childNodes[k];
          if (node.nodeType === Node.ELEMENT_NODE) {
            return node;
          }
        }
        return null;
      }
    });
  }
})();

/*
 * Copyright (c) Mev-Rael (mevrael@gmail.com)
 * BunnyJS (https://bunnyjs.com)
 */

// Normalize all native Interfaces and Prototypes
// across all browsers
// According to WHAT WG HTML specification
// and ECMA-262 specification

// add Node.prototype.firstElementChild
// https://dom.spec.whatwg.org/#dom-parentnode-firstelementchild
