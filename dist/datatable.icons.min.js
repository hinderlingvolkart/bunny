
Object.assign(DataTable, {
  initIcons: function initIcons(datatable) {
    this.onBeforeRedraw(datatable, function () {
      Spinner.fadePage();
    });
    this.onRedraw(datatable, function (res, err) {
      Spinner.unfadePage();
    });
  }
});
